<Window x:Class="FluxRead.Views.MiniCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FluxRead — Capturing"
        Width="380" Height="300" ResizeMode="NoResize"
        Topmost="True" ShowInTaskbar="True"
        Background="{StaticResource SurfaceBrush}"
        WindowStartupLocation="Manual">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding StateText}" FontSize="18" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Text="{Binding ProgressText}" Style="{StaticResource SubtleText}" Margin="0,4,0,0"/>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,10,0,0" Padding="6">
            <Image Source="{Binding LastThumbnail}" Stretch="Uniform"/>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="PauseButton" Content="{Binding PauseLabel}" Style="{StaticResource SecondaryButton}"
                    MinWidth="90" Click="OnPauseToggle"/>
            <Button Content="Cancel" Style="{StaticResource DangerButton}" MinWidth="90" Margin="10,0,0,0"
                    Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
