<Window x:Class="FluxRead.Views.FramePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FluxRead — Choose a frame"
        Icon="/Assets/AppIcon.ico"
        SizeToContent="WidthAndHeight" MaxWidth="900" ResizeMode="NoResize"
        Style="{StaticResource FluxWindow}"
        WindowStartupLocation="CenterOwner">
    <DockPanel x:Name="RootContent" Margin="24">
        <TextBlock DockPanel.Dock="Top" Text="Multiple frames found" Style="{StaticResource HeadingText}"/>
        <TextBlock DockPanel.Dock="Top" Text="Pick the one to capture." Style="{StaticResource SubtleText}" Margin="0,4,0,16"/>

        <Button DockPanel.Dock="Bottom" Content="Cancel" HorizontalAlignment="Right" Margin="0,16,0,0"
                Style="{StaticResource SecondaryButton}" MinWidth="96" Click="OnCancel"/>

        <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto">
            <ItemsControl x:Name="Candidates">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Tag="{Binding Index}" Click="OnPick" Style="{StaticResource SecondaryButton}"
                                MinHeight="0" Padding="8" Margin="0,0,12,0">
                            <StackPanel Width="220">
                                <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="6" Padding="4">
                                    <Image Source="{Binding Thumbnail}" MaxHeight="150" Stretch="Uniform"/>
                                </Border>
                                <TextBlock Text="{Binding Label}" HorizontalAlignment="Center" Margin="0,8,0,0"
                                           FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
