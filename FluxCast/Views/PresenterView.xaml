<UserControl
    x:Class="FluxCast.Views.PresenterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#FF262A30" RowDefinitions="72,Auto,*,Auto">

        <Border Grid.Row="0" Background="#FF16181D">
            <!-- Next keeps its text label: the receiver's OCR locates that button by the word "Next". -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="6">
                <Button Command="{x:Bind Vm.FirstCommand}" IsTabStop="False" AutomationProperties.Name="First frame"
                        ToolTipService.ToolTip="First frame"
                        Style="{StaticResource SecondaryButton}" MinWidth="50" Padding="0" MinHeight="44">
                    <Path Data="M11,2 L3,8 L11,14 M2,2 L2,14" Stroke="{ThemeResource TextPrimaryBrush}"
                          StrokeThickness="1.6" StrokeLineJoin="Round"/>
                </Button>
                <Button Command="{x:Bind Vm.BackCommand}" IsTabStop="False"
                        Style="{StaticResource SecondaryButton}" MinWidth="104" MinHeight="44">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Path Data="M9,2 L3,8 L9,14" Stroke="{ThemeResource TextPrimaryBrush}" StrokeThickness="1.6"
                              StrokeLineJoin="Round" VerticalAlignment="Center"/>
                        <TextBlock Text="Back" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <TextBlock Text="Frame" FontSize="15" Foreground="White" VerticalAlignment="Center" Margin="10,0,2,0"/>
                <TextBox Text="{x:Bind Vm.GotoText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Width="56" Height="44" Padding="4,0" TextAlignment="Center"
                         VerticalContentAlignment="Center" KeyDown="OnGotoKeyDown"
                         ToolTipService.ToolTip="Go to frame number"/>
                <TextBlock Text="{x:Bind TotalLabel}" FontSize="15" Foreground="White" VerticalAlignment="Center"/>
                <Button Content="Go" Command="{x:Bind Vm.GotoCommand}" IsTabStop="False"
                        Style="{StaticResource SecondaryButton}" MinHeight="44"/>

                <Button Command="{x:Bind Vm.NextCommand}" IsTabStop="False" Margin="10,0,0,0"
                        Style="{StaticResource PrimaryButton}" MinWidth="104" MinHeight="44">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="Next" FontSize="16" VerticalAlignment="Center"/>
                        <Path Data="M3,2 L9,8 L3,14" Stroke="White" StrokeThickness="1.6"
                              StrokeLineJoin="Round" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Command="{x:Bind Vm.LastCommand}" IsTabStop="False" AutomationProperties.Name="Last frame"
                        ToolTipService.ToolTip="Last frame"
                        Style="{StaticResource SecondaryButton}" MinWidth="50" Padding="0" MinHeight="44">
                    <Path Data="M3,2 L11,8 L3,14 M12,2 L12,14" Stroke="{ThemeResource TextPrimaryBrush}"
                          StrokeThickness="1.6" StrokeLineJoin="Round"/>
                </Button>
            </StackPanel>
        </Border>

        <Border x:Name="SizeWarning" Grid.Row="1" Background="#CCB91C1C" Padding="14,7" Visibility="Collapsed">
            <TextBlock Text="Window is small — enlarge it so the whole frame is visible at native size."
                       Foreground="White" FontSize="12.5" HorizontalAlignment="Center" TextAlignment="Center"/>
        </Border>

        <!-- Sized in code-behind: the frame must land on whole device pixels, never be resampled. -->
        <Grid x:Name="FrameArea" Grid.Row="2" Margin="10">
            <Image x:Name="FrameImage" Source="{x:Bind Vm.CurrentFrame, Mode=OneWay}" Stretch="None"
                   ImageOpened="OnFrameOpened" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="3" Background="#FF16181D" Padding="0,7">
            <TextBlock Text="Keep this window still during transfer" FontSize="10.5"
                       Foreground="#FF8A9199" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
