using System.Windows;

namespace FluxCast;

/// <summary>
/// Shell window. Phase 6 replaces the placeholder content with the encode setup,
/// progress, and presenter views.
/// </summary>
public partial class MainWindow : Window
{
    /// <summary>
    /// Initializes a new instance of the <see cref="MainWindow"/> class.
    /// </summary>
    public MainWindow()
    {
        InitializeComponent();
    }
}
